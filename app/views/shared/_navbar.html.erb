<nav class="w-full bg-gray-900 shadow mb-2">
  <div class="max-w-5xl mx-auto px-4 py-3 flex items-center justify-between">
    <div>
      <a href="/" class="text-2xl font-bold text-blue-400 hover:text-blue-300">WorkTracker</a>
    </div>
    <div class="flex gap-4">
      <% if user_signed_in? %>
        <%= link_to "Work Log", log_entries_path, class: "text-gray-200 hover:text-blue-400 font-semibold px-3 py-2 rounded transition" %>
        <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "bg-red-600 hover:bg-red-500 text-white font-semibold px-3 py-2 rounded shadow transition", form_class: "inline", data: { turbo: false } %>
      <% else %>
        <%= link_to "Log In", new_user_session_path, class: "text-gray-200 hover:text-blue-400 font-semibold px-3 py-2 rounded transition", data: { turbo: false } %>
        <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-200 hover:text-blue-400 font-semibold px-3 py-2 rounded transition", data: { turbo: false } %>
      <% end %>
    </div>
  </div>
</nav>
